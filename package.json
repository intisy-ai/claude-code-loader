{
  "name": "claude-code-loader",
  "version": "1.11.1",
  "description": "TUI launcher and cc shell command for Claude Code.",
  "main": "dist/plugin.js",
  "type": "module",
  "license": "MIT",
  "author": "intisy",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/intisy-ai/claude-code-loader.git"
  },
  "keywords": [
    "claude",
    "claude-code",
    "plugin",
    "launcher",
    "tui"
  ],
  "files": [
    "dist/",
    "cairn.json",
    "icon.svg",
    "core-loader/dist/",
    "core-loader/api/dist/",
    "core-loader/api/package.json",
    "core/dist/",
    "claude-code-proxy/dist/",
    "claude-code-proxy/core-proxy/dist/",
    "claude-code-proxy/anthropic-translator/dist/",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=20.19.0"
  },
  "claudeHub": {
    "daemon": {
      "script": "dist/proxy.js",
      "runtime": "node",
      "port": 34567,
      "healthCheckUrl": "http://127.0.0.1:34567/health"
    }
  },
  "scripts": {
    "build": "npm --prefix claude-code-proxy install && npm --prefix claude-code-proxy run build && npm --prefix core-loader install && npm --prefix core-loader run build && npm --prefix core install && npm --prefix core run build && npx tsc && node build.mjs",
    "postbuild": "node dist/plugin.js readme",
    "prepublishOnly": "npm run build",
    "test": "npm run build && vitest run"
  },
  "devDependencies": {
    "esbuild": "^0.25.12",
    "@types/node": "^25.9.1",
    "plugin-updater": "^1.0.21",
    "typescript": "^6.0.3",
    "vitest": "^3.2.6"
  },
  "dependencies": {
    "@intisy-ai/core": "file:core",
    "@intisy-ai/core-loader": "file:core-loader",
    "@intisy-ai/claude-code-proxy": "file:claude-code-proxy"
  }
}
